services:
  - type: web
    name: bolao-copa-junina-w3g
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
    disk:
      name: dados
      mountPath: /var/data
      sizeGB: 1
